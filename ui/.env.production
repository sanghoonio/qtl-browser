# Production reads /data on its own origin: the Worker (worker.js) serves it from the R2
# binding. Set VITE_DATA_BASE to a URL only to point a build at a different host, e.g. the
# bucket's public r2.dev URL. `npm run preview` serves local data at /data the same way.
VITE_DATA_BASE=
